{
  "name": "persona-builder",
  "version": "1.0.0",
  "description": "Build a Firefox Persona with browser history, and snapshots of HTML.",
  "main": "background.js",
  "scripts": {
    "start": "./bin/run-web-ext.sh",
    "ts": "tsc --noEmit",
    "lint": "web-ext lint --config ./.web-ext-config.mjs",
    "build": "web-ext build --overwrite-dest --config ./.web-ext-config.mjs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/firefox-webext-browser": "^143.0.0",
    "typescript": "^5.9.3",
    "web-ext": "^9.2.0"
  }
}
