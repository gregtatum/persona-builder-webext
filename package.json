{
  "name": "persona-builder",
  "version": "1.0.0",
  "description": "Build a Firefox Persona with browser history, and snapshots of HTML.",
  "main": "background.js",
  "scripts": {
    "start": "web-ext run --browser-console --keep-profile-changes --config ./.web-ext-config.cjs",
    "lint": "web-ext lint --config ./.web-ext-config.cjs",
    "build": "web-ext build --overwrite-dest --config ./.web-ext-config.cjs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "web-ext": "^9.2.0"
  }
}
